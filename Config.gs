
// Config.gs — ค่าตั้งค่าพื้นฐานทั้งหมด
// 📌 ไฟล์นี้เก็บ API Keys และ ID ต่างๆ
//    แก้ไขค่าในไฟล์นี้ไฟล์เดียวเมื่อต้องเปลี่ยน Token/Key



 * LINE Messaging API — Channel Access Token
 * ใช้สำหรับส่งข้อความตอบกลับผ่าน LINE API

var LINE_ACCESS_TOKEN = 'xxx';


 * Google Sheets — Spreadsheet ID
 * ชีทหลักที่เก็บข้อมูล Loss (แท็บ 'Chatbot Database') และข้อมูลไฟฟ้า (แท็บ 'พลังงาน')

var SPREADSHEET_ID = 'xxx';

/**
 * OpenAI API Key
 * ใช้เรียก GPT-4o-mini สำหรับวิเคราะห์ข้อมูลปัญหาในโรงงาน
 */
var OPENAI_API_KEY = 'xxx';
